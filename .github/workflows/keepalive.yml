name: Supabase Keepalive Ping

on:
  schedule:
    # Runs at 00:00 UTC every 3 days
    - cron: '0 0 */3 * *'
  # Also allow manual triggering
  workflow_dispatch:

jobs:
  ping-supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          curl -H "apikey: sb_publishable_7ihTP84sdeKancFDaCep5w_ysBVwOMy" \
               -H "Authorization: Bearer sb_publishable_7ihTP84sdeKancFDaCep5w_ysBVwOMy" \
               "https://nhxkuppugjxtbmeimqyq.supabase.co/rest/v1/user_data?limit=1"
